/**
 * settings/sections/shortcuts/scripts/shortcutsHandler.js
 * Shortcuts section is static — no event binding needed.
 * Placeholder for future custom shortcut editing.
 */

const ShortcutsHandler = (() => {

  function bind(_container, _settings) {
    // Static section — no interactive elements currently
    // Future: custom shortcut remapping
  }

  return { bind };

})();
